# Python environment
.venv/
__pycache__/
*.pyc

# Raw pipeline outputs (large working files, regenerated by notebooks/01_generate_data.py,
# not needed by the deployed dashboard, which only reads dashboard/dashboard_data/)
data/

# Real secrets, never commit these. Only secrets.toml.example is tracked.
.streamlit/secrets.toml

# OS / editor cruft
.DS_Store
*.log

# Local Claude Code housekeeping, not part of the project
.claude/

# Personal planning notes, not meant for the public repo
PriceIQ_Roadmap.md
PriceIQ_Roadmap.pdf
